{
  "name": "obelisk",
  "version": "0.1.0",
  "description": "Self-hosted AT Protocol record archive with keyword and vector search",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "migrate": "bun run src/db/migrate.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@atproto/tap": "^0.3.5",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "drizzle-kit": "^0.31.10"
  }
}
