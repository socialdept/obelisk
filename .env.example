# Postgres (docker compose default)
DATABASE_URL=postgres://reservoir:reservoir@localhost:5432/reservoir

# Tab websocket (docker compose default)
TAB_WS_URL=ws://localhost:2480

# Ollama (host)
OLLAMA_URL=http://127.0.0.1:11434

# HTTP API
PORT=3000
